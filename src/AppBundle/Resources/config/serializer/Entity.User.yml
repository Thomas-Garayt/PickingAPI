AppBundle\Entity\User:
    exclusion_policy: none
    properties:
        id:
            groups: ['user', 'auth-token']
        identifier:
            groups: ['user', 'auth-token']
        firstname:
            groups: ['user', 'auth-token']
        lastname:
            groups: ['user', 'auth-token']
        email:
            groups: ['user', 'auth-token']
        username:
            groups: ['user', 'auth-token']
        caracteristic:
            groups: ['user']
