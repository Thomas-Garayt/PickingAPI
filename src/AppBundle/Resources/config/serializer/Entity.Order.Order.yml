AppBundle\Entity\Order\Order:
    exclusion_policy: none
    properties:
        id:
            groups: ['order', 'course']
        reference:
            groups: ['order']
        customer:
            groups: ['order']
        products:
            groups: ['order']
        preparations:
            groups: ['order']
        status:
            groups: ['order']
        price:
            groups: ['order']
        weight:
            groups: ['order']
        createdAt:
            groups: ['order']
